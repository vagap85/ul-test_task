# Блок 5: 1С (тяжело далось, я не 1С-разработчик)

## Часть 1. Запрос к регистру сведений «Цены доставки»

```1c
&НаСервере
Функция РассчитатьСтоимость(ДатаЗаказа, ТипКлиентаСсылка, ОбщийВес)
    
    Запрос = Новый Запрос;
    Запрос.Текст = "
    |ВЫБРАТЬ ПЕРВЫЕ 1
    |    ЦеныДоставки.Стоимость КАК Стоимость
    |ИЗ
    |    РегистрСведений.ЦеныДоставки КАК ЦеныДоставки
    |ГДЕ
    |    ЦеныДоставки.ТипКлиента = &ТипКлиента
    |    И ЦеныДоставки.Период <= &ДатаЗаказа
    |    И ЦеныДоставки.ВесОт < &ОбщийВес
    |    И &ОбщийВес <= ЦеныДоставки.ВесДо
    |УПОРЯДОЧИТЬ ПО
    |    ЦеныДоставки.Период УБЫВ,
    |    ЦеныДоставки.ВесОт УБЫВ
    |";
    
    Запрос.УстановитьПараметр("ДатаЗаказа", ДатаЗаказа);
    Запрос.УстановитьПараметр("ТипКлиента", ТипКлиентаСсылка);
    Запрос.УстановитьПараметр("ОбщийВес", ОбщийВес);
    
    Результат = Запрос.Выполнить();
    Если Результат.Пустой() Тогда
        Возврат Неопределено;
    КонецЕсли;
    
    Выборка = Результат.Выбрать();
    Выборка.Следующий();
    Возврат Выборка.Стоимость;
    
КонецФункции

Часть 2. Ошибки в коде из задания (нашёл 7)
№	Ошибка	Почему плохо	Исправление
1	МЕЖДУ ВесОт И ВесДо	Вес 10 попадает в два интервала [0,10] и [10,20]	ВесОт < Вес И Вес <= ВесДо
2	ТекущаяДата() вместо параметра	Игнорирует дату заказа	использовать переданный параметр
3	НайтиПоРеквизиту(...) без проверки	Клиент не найден → ошибка	проверить на Неопределено
4	Нет сортировки по ВесОт	При одинаковом периоде результат случайный	добавить ВесОт УБЫВ
5	Нет ПЕРВЫЕ 1	Выбираются все записи → лишняя нагрузка	добавить ВЫБРАТЬ ПЕРВЫЕ 1
6	Возврат 0 при отсутствии цены	0 = бесплатно, не отличить от ошибки	возвращать Неопределено
7	Нет проверки пустой табличной части	Вес = 0, а это может быть ошибкой	Если СуммаВес = 0 Тогда Возврат